import computational_monads.oracle_comp

-- structure security_experiment : Type 1 :=
-- (input : ℕ → Type)
-- (output : ℕ → Type)
-- (oracle_access : oracle_spec)
-- (adversary : Π (n : ℕ), input n → oracle_comp oracle_access (output n))
-- (experiment )